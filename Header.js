// eslint-disable-next-line no-unused-vars
const Header = (props) => {
    // eslint-disable-next-line no-unused-vars
    const {title} = props;

    return (
        <header><h2>{title}</h2></header>
    )
}

export default Header;